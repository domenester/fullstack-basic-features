{
  "name": "guig-work-infra",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "infra:up:dev": "docker-compose -f docker-compose.dev.yml up",
    "infra:up": "docker-compose up",
    "infra:down": "docker-compose down",
    "infra:reload": "docker-compose down && docker-compose up --build -V",
    "infra:reload:dev": "docker-compose down && docker-compose -f docker-compose.dev.yml up --build -V"
  }
}
